# Robotic Mower Hub (hub.robomaintain.com)

Hub UI + headless Shopify shop/cart/checkout (Storefront API via server proxy).

## Local
```bash
npm install
npm run dev
```

## Env
Copy `.env.example` → `.env.local` and fill values.
